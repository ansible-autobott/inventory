all:
  children:
    machines:
      hosts:
        example.com:        ## fqdn of the instance
          ansible_python_interpreter: /usr/bin/python3.11
    #============================================================================================================
    # host based on type
    #============================================================================================================
    linux_servers:
      children:
        machines:
